apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
}

clean{
    doFirst {
        delete file('../sandbox/rabbit-chronicler.jar')
    }
}

jar{
    doLast {
        copy {
            from 'build/libs/rabbit-chronicler.jar'
            into "../sandbox/"
        }
    }
}
